function Logo() {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "1.2rem",
        color: "#fff",
        fontWeight: "700",
        padding: "1rem",
        borderRadius: "1000rem",
        background: "rgb(73 73 73)",
        width: "4rem",
        height: "4rem",
        display: "inline-flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      M&N
    </div>
  );
}

export default Logo;
